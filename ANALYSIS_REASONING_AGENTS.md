# Анализ применимости методов reasoning-агентов к проекту Semantic Contamination

**Дата анализа:** 2025-11-15
**Версия проекта:** 1.0 (коммит 373d670)

## Оглавление
1. [Текущее состояние проекта](#текущее-состояние-проекта)
2. [Критический анализ по категориям](#критический-анализ-по-категориям)
3. [Приоритетные рекомендации](#приоритетные-рекомендации)
4. [Roadmap внедрения](#roadmap-внедрения)
5. [Оценка трудозатрат](#оценка-трудозатрат)

---

## Текущее состояние проекта

### Реализованная архитектура

**Компоненты:**
- **ExtractorAgent** (`src/agents/extractor.py`): Извлечение тезисов из документов (простые эвристики)
- **AlignerAgent** (`src/agents/aligner.py`): Семантическое сопоставление (Jaccard similarity)
- **JudgeAgent** (`src/agents/judge.py`): Парное судейство по 5 критериям (эвристическая оценка)
- **VerifierAgent** (`src/agents/verifier.py`): Фактчекинг через CoVe (заглушка без реального веб-поиска)
- **TextMergeOrchestrator** (`src/orchestrator.py`): Последовательная оркестрация агентов

**Текущие зависимости:**
```
pandas>=2.0.0
numpy>=1.24.0
regex>=2023.0.0
python-dateutil>=2.8.0
```

**Критические ограничения:**
1. ❌ Нет интеграции с LLM API (OpenAI, Anthropic, etc.)
2. ❌ Нет реального веб-поиска для верификации фактов
3. ❌ Используются примитивные эвристики вместо LLM-инференса
4. ❌ Нет специализированных NLI/entailment моделей
5. ❌ Нет ансамблирования или парного ранжирования
6. ❌ Линейный пайплайн без ветвления/поиска по пространству решений
7. ❌ Нет детекции галлюцинаций

---

## Критический анализ по категориям

### 1. Алгоритмы поиска по пространству рассуждений (ToT, LATS, ReAct)

#### 🔴 ВЫСОКИЙ ПРИОРИТЕТ: ReAct для VerifierAgent

**Проблема:** `VerifierAgent._verify_question()` (строка 174-204) — заглушка, возвращающая `status: 'uncertain'`

**Решение:**
- **Репозиторий:** [`ysymyth/ReAct`](https://github.com/ysymyth/ReAct)
- **Интеграция:** Добавить ReAct-цикл "Рассуждение → Действие (веб-поиск) → Наблюдение"

**Оценка эффекта:**
- ✅ **Критично:** Превращает заглушку в рабочую функцию
- ✅ **Простота:** Средняя (нужен API для веб-поиска, например SerpAPI)
- ✅ **ROI:** 🟢 **ОЧЕНЬ ВЫСОКИЙ**

**Код-пример интеграции:**
```python
from react import ReActAgent

class VerifierAgent(BaseAgent):
    def __init__(self, ...):
        super().__init__(prompt_template_path)
        self.react_agent = ReActAgent(
            llm=your_llm_api,  # OpenAI/Anthropic
            tools=[WebSearchTool(api_key=...)]
        )

    def _verify_question(self, claim_id, question, claim):
        # ReAct-цикл: Thought → Action → Observation
        result = self.react_agent.run(
            task=f"Проверь факт: {question}",
            max_steps=5
        )
        return {
            'claim_id': claim_id,
            'question': question,
            'status': self._parse_status(result),
            'source': result.sources[0] if result.sources else '',
            ...
        }
```

**Альтернатива (минимальная):**
- Использовать `mukulpatnaik/webgpt-cli` для простого WebGPT-подхода
- Легче интегрировать, но менее гибко

---

#### 🟡 СРЕДНИЙ ПРИОРИТЕТ: ToT для JudgeAgent (множественные стратегии оценки)

**Проблема:** `JudgeAgent._judge_pair()` использует фиксированную эвристику, нет исследования альтернативных рассуждений

**Решение:**
- **Репозиторий:** [`kyegomez/tree-of-thoughts`](https://github.com/kyegomez/tree-of-thoughts) (более прикладной, чем princeton-nlp)
- **Применение:** Генерировать несколько "веток" рассуждений о том, какой тезис лучше, затем выбирать лучшую

**Оценка эффекта:**
- ✅ **Польза:** Повышает качество судейства, снижает bias
- ⚠️ **Сложность:** Высокая (требует 3-5 LLM вызовов на пару)
- ⚠️ **Стоимость:** Увеличивает API-расходы в 3-5 раз
- ✅ **ROI:** 🟡 **СРЕДНИЙ** (улучшение качества vs стоимость)

**Рекомендация:** Внедрять после ReAct, на этапе оптимизации качества

---

#### 🟢 НИЗКИЙ ПРИОРИТЕТ: LATS / полный ToT для ExtractorAgent

**Оценка:**
- ❌ **Избыточно:** Экстракция тезисов — относительно простая задача
- ❌ **Стоимость:** Очень высокая (генерация дерева для каждого абзаца)
- ❌ **ROI:** 🔴 **НИЗКИЙ**

**Рекомендация:** **НЕ внедрять**. Использовать simple CoT или single-pass extraction

---

#### 🟡 СРЕДНИЙ ПРИОРИТЕТ: CCoT (Concise Chain-of-Thought)

**Проблема:** Текущие агенты не используют chain-of-thought вообще

**Решение:**
- **Репозиторий:** [`matthewrenze/jhu-concise-cot`](https://github.com/matthewrenze/jhu-concise-cot)
- **Применение:** Добавить компактный CoT к промптам всех агентов

**Оценка эффекта:**
- ✅ **Простота:** Очень простая (модификация промптов)
- ✅ **Стоимость:** Минимальная (+10-20% токенов)
- ✅ **ROI:** 🟢 **ВЫСОКИЙ**

**Пример для AlignerAgent:**
```markdown
# В aligner_prompt.md добавить:

Прежде чем определить отношение, кратко рассуди:
1. Какие ключевые термины совпадают?
2. Есть ли противоречащие факты?
3. Различается ли scope применимости?

Затем дай окончательный ответ.
```

---

### 2. Ансамблирование и Mixture-of-Agents

#### 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ: PairRM для JudgeAgent

**Проблема:** `JudgeAgent._determine_winner()` (строки 268-304) использует примитивный подсчет побед

**Решение:**
- **Модель:** [`llm-blender/PairRM`](https://huggingface.co/llm-blender/PairRM) на HuggingFace
- **Применение:** Использовать PairRM для парного ранжирования тезисов вместо эвристик

**Оценка эффекта:**
- ✅ **Критично:** Заменяет весь эвристический блок на SOTA модель
- ✅ **Простота:** Средняя (требует transformers, но inference простой)
- ✅ **ROI:** 🟢 **ОЧЕНЬ ВЫСОКИЙ**

**Код-интеграция:**
```python
from transformers import AutoTokenizer, AutoModelForSequenceClassification
import torch

class JudgeAgent(BaseAgent):
    def __init__(self, ...):
        super().__init__(...)
        self.pairrm_tokenizer = AutoTokenizer.from_pretrained("llm-blender/PairRM")
        self.pairrm_model = AutoModelForSequenceClassification.from_pretrained(
            "llm-blender/PairRM"
        )

    def _judge_pair(self, claim_a, claim_b, pair, order='AB'):
        # Вместо эвристик - PairRM
        inputs = self.pairrm_tokenizer(
            claim_a['claim'],
            claim_b['claim'],
            return_tensors='pt',
            truncation=True,
            max_length=512
        )

        with torch.no_grad():
            logits = self.pairrm_model(**inputs).logits
            # logits[0] > 0 → A лучше, иначе B
            winner = 'A' if logits[0] > 0 else 'B'

        return {'pair_id': pair['pair_id'], 'winner': winner, ...}
```

**Преимущества:**
- 🚀 Bidirectional cross-attention (лучше, чем pointwise оценка)
- 🚀 Обучена на реальных парных сравнениях
- 🚀 Не требует API-вызовов (локальный inference)

---

#### 🟡 СРЕДНИЙ ПРИОРИТЕТ: LLM-Blender (PairRM + GenFusion)

**Проблема:** После судейства нет финального слияния тезисов

**Решение:**
- **Репозиторий:** [`yuchenlin/LLM-Blender`](https://github.com/yuchenlin/LLM-Blender)
- **Применение:**
  1. PairRM для ранжирования
  2. GenFusion для слияния топ-N тезисов в финальный текст

**Оценка эффекта:**
- ✅ **Польза:** Автоматизирует финальный синтез (сейчас ручной через `prompt_01.md`)
- ⚠️ **Сложность:** Высокая (требует FlanT5-XXL для GenFusion)
- ⚠️ **ROI:** 🟡 **СРЕДНИЙ**

**Рекомендация:** Внедрять после PairRM, если нужна полная автоматизация

---

#### 🟢 СРЕДНИЙ ПРИОРИТЕТ: Mixture-of-Agents (MoA)

**Проблема:** Один агент выполняет одну задачу, нет итеративного улучшения

**Решение:**
- **Репозиторий:** [`togethercomputer/MoA`](https://github.com/togethercomputer/MoA)
- **Применение:** Многослойная архитектура, где каждый слой агентов потребляет выходы предыдущего

**Концептуальная схема для проекта:**
```
Слой 1: [Extractor-v1, Extractor-v2, Extractor-v3]  ← разные промпты/модели
         ↓
Слой 2: [Aggregator] ← слияние экстрагированных тезисов
         ↓
Слой 3: [Aligner + Judge]
         ↓
Финальный выход
```

**Оценка эффекта:**
- ✅ **Польза:** Повышает robustness через diversity
- ⚠️ **Сложность:** Очень высокая (архитектурный редизайн)
- ⚠️ **Стоимость:** 3-5x API-вызовов
- ⚠️ **ROI:** 🟡 **СРЕДНИЙ** (для критичных задач)

**Рекомендация:** Рассмотреть для production, если нужна максимальная надежность

---

### 3. Детекция галлюцинаций и верификация

#### 🔴 ВЫСОКИЙ ПРИОРИТЕТ: SelfCheckGPT для ExtractorAgent

**Проблема:** `ExtractorAgent._is_substantive()` (строки 162-189) — примитивная проверка, нет детекции галлюцинаций при извлечении тезисов

**Решение:**
- **Репозиторий:** [`potsawee/selfcheckgpt`](https://github.com/potsawee/selfcheckgpt)
- **Применение:** После извлечения тезисов проверить их на консистентность через sampling

**Оценка эффекта:**
- ✅ **Критично:** Предотвращает попадание галлюцинаций в пайплайн
- ✅ **Простота:** Средняя (требует multiple sampling)
- ✅ **ROI:** 🟢 **ВЫСОКИЙ**

**Код-интеграция:**
```python
from selfcheckgpt.modeling_selfcheck import SelfCheckNLI

class ExtractorAgent(BaseAgent):
    def __init__(self, ...):
        super().__init__(...)
        self.selfcheck = SelfCheckNLI(device='cuda')

    def _extract_claims_from_section(self, section_content, ...):
        # 1. Генерируем тезисы (основной проход)
        claims = self._llm_extract(section_content)

        # 2. Генерируем N samples для проверки
        samples = [self._llm_extract(section_content) for _ in range(5)]

        # 3. SelfCheck для каждого тезиса
        verified_claims = []
        for claim in claims:
            score = self.selfcheck.predict(
                sentences=[claim['claim']],
                sampled_passages=samples
            )
            if score[0] < 0.5:  # низкая галлюцинация
                verified_claims.append(claim)

        return verified_claims
```

**Альтернатива (проще):**
- Использовать модель [`bond005/xlm-roberta-xl-hallucination-detector`](https://huggingface.co/bond005/xlm-roberta-xl-hallucination-detector)
- Просто бинарная классификация без multiple sampling

---

#### 🟡 СРЕДНИЙ ПРИОРИТЕТ: Chain-of-Verification (CoVe) для VerifierAgent

**Текущее состояние:** `VerifierAgent` упоминает CoVe в комментариях, но не реализует

**Решение:**
- **Репозиторий:** [`ritun16/chain-of-verification`](https://github.com/ritun16/chain-of-verification) или [`rachittshah/CoV-langchain`](https://github.com/rachittshah/CoV-langchain)
- **Применение:**
  1. Базовый ответ (extraction)
  2. Генерация verification questions
  3. Ответы на вопросы
  4. Переписывание ответа

**Оценка эффекта:**
- ✅ **Польза:** Структурированная верификация вместо ad-hoc
- ⚠️ **Сложность:** Средняя
- ✅ **ROI:** 🟢 **ВЫСОКИЙ** (если внедрять вместе с ReAct)

**Рекомендация:** Интегрировать вместе с ReAct для VerifierAgent

---

### 4. Оркестрация и многоагентные системы

#### 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ: LangGraph для замены линейного оркестратора

**Проблема:** `TextMergeOrchestrator.run_pipeline()` (строки 41-125) — жесткий линейный пайплайн, нельзя:
- Повторить шаг при ошибке
- Ветвиться по условиям
- Параллельно запускать агентов

**Решение:**
- **Репозиторий:** [`langchain-ai/langgraph`](https://github.com/langchain-ai/langgraph)
- **Применение:** Перевести пайплайн на StateGraph

**Оценка эффекта:**
- ✅ **Критично:** Делает систему production-ready
- ✅ **Простота:** Средняя (требует рефакторинга, но LangGraph хорошо документирован)
- ✅ **ROI:** 🟢 **ОЧЕНЬ ВЫСОКИЙ**

**Концептуальная схема:**
```python
from langgraph.graph import StateGraph

class PipelineState:
    documents: List[Dict]
    claims: List[Dict]
    pairs: List[Dict]
    judgments: List[Dict]
    evidence: List[Dict]

workflow = StateGraph(PipelineState)

# Узлы
workflow.add_node("extract", extractor_node)
workflow.add_node("align", aligner_node)
workflow.add_node("judge", judge_node)
workflow.add_node("verify", verifier_node)

# Рёбра с условиями
workflow.add_edge("extract", "align")
workflow.add_conditional_edges(
    "align",
    should_verify_conflicts,  # если есть конфликты → verify
    {
        "verify": "verify",
        "judge": "judge"
    }
)

app = workflow.compile()
result = app.invoke(initial_state)
```

**Преимущества:**
- 🚀 Циклы (retry, iterative refinement)
- 🚀 Условное ветвление
- 🚀 Персистентность состояния
- 🚀 Интеграция с LangSmith для debugging

---

#### 🟡 СРЕДНИЙ ПРИОРИТЕТ: AutoGen для multi-agent debate

**Применение:** Добавить "debate" между Judge-агентами для спорных случаев

**Решение:**
- **Репозиторий:** [`microsoft/autogen`](https://github.com/microsoft/autogen)
- **Сценарий:** Если `JudgeAgent._resolve_disagreement()` возвращает `uncertain`, запустить дебаты между агентами

**Оценка эффекта:**
- ✅ **Польза:** Снижает uncertain cases
- ⚠️ **Сложность:** Высокая
- ⚠️ **ROI:** 🟡 **СРЕДНИЙ**

**Рекомендация:** Рассмотреть после внедрения LangGraph

---

#### 🟢 НИЗКИЙ ПРИОРИТЕТ: DSPy для оптимизации промптов

**Проблема:** Промпты (`prompts/*.md`) статичные, не оптимизируются

**Решение:**
- **Репозиторий:** [`stanfordnlp/dspy`](https://github.com/stanfordnlp/dspy)
- **Применение:** Автоматическая оптимизация промптов на валидационном датасете

**Оценка эффекта:**
- ✅ **Польза:** Улучшение качества без ручного tuning
- ⚠️ **Сложность:** Очень высокая (требует переписывания агентов)
- ⚠️ **ROI:** 🟡 **СРЕДНИЙ** (долгосрочная перспектива)

**Рекомендация:** Рассмотреть после стабилизации базового пайплайна

---

### 5. RAG и веб-поиск

#### 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ: LangChain + SerpAPI для VerifierAgent

**Проблема:** `VerifierAgent._verify_question()` не имеет реального веб-поиска

**Решение:**
```python
from langchain.tools import SerpAPIWrapper
from langchain.agents import initialize_agent, Tool

class VerifierAgent(BaseAgent):
    def __init__(self, ...):
        super().__init__(...)
        self.search = SerpAPIWrapper(serpapi_api_key=...)

    def _verify_question(self, claim_id, question, claim):
        # Веб-поиск
        search_results = self.search.run(question)

        # Анализ с LLM
        analysis = self.llm.run(f"""
        Вопрос: {question}
        Результаты поиска: {search_results}

        Определи статус: supported/refuted/uncertain
        """)

        return {...}
```

**Оценка эффекта:**
- ✅ **Критично:** Превращает заглушку в working solution
- ✅ **Простота:** Простая (LangChain well-documented)
- ✅ **ROI:** 🟢 **ОЧЕНЬ ВЫСОКИЙ**

---

#### 🟡 СРЕДНИЙ ПРИОРИТЕТ: LlamaIndex для RAG (локальные документы)

**Применение:** Если нужно проверять факты по внутренней базе документов (не веб)

**Оценка:**
- ⚠️ **Актуальность:** Зависит от use case (веб vs internal docs)
- ✅ **ROI:** 🟡 **СРЕДНИЙ** (если есть internal knowledge base)

---

## Приоритетные рекомендации

### 🔴 Tier 1: КРИТИЧНЫЕ (внедрить немедленно)

| Компонент | Библиотека/Репозиторий | Эффект | Сложность | ROI |
|-----------|------------------------|--------|-----------|-----|
| **VerifierAgent (веб-поиск)** | `langchain` + `SerpAPI` + `ysymyth/ReAct` | Превращает заглушку в working solution | Средняя | 🟢🟢🟢 |
| **JudgeAgent (ранжирование)** | `llm-blender/PairRM` (HF) | Заменяет эвристики на SOTA модель | Средняя | 🟢🟢🟢 |
| **Orchestrator (граф)** | `langchain-ai/langgraph` | Production-ready пайплайн | Средняя | 🟢🟢🟢 |
| **ExtractorAgent (галлюцинации)** | `potsawee/selfcheckgpt` | Фильтрует галлюцинации | Средняя | 🟢🟢 |

**Оценка трудозатрат:** 40-60 часов разработки

---

### 🟡 Tier 2: ВАЖНЫЕ (после Tier 1)

| Компонент | Библиотека/Репозиторий | Эффект | Сложность | ROI |
|-----------|------------------------|--------|-----------|-----|
| **Все агенты (CoT)** | `matthewrenze/jhu-concise-cot` | Улучшение reasoning | Низкая | 🟢🟢 |
| **VerifierAgent (CoVe)** | `ritun16/chain-of-verification` | Структурированная верификация | Средняя | 🟢🟢 |
| **Финальный синтез** | `yuchenlin/LLM-Blender` (GenFusion) | Автоматический синтез | Высокая | 🟡 |
| **JudgeAgent (ToT)** | `kyegomez/tree-of-thoughts` | Более глубокое рассуждение | Высокая | 🟡 |

**Оценка трудозатрат:** 30-50 часов разработки

---

### 🟢 Tier 3: ОПЦИОНАЛЬНЫЕ (долгосрочная перспектива)

| Компонент | Библиотека/Репозиторий | Эффект | Сложность | ROI |
|-----------|------------------------|--------|-----------|-----|
| **Multi-agent debate** | `microsoft/autogen` | Разрешение спорных случаев | Высокая | 🟡 |
| **Mixture-of-Agents** | `togethercomputer/MoA` | Robustness через diversity | Очень высокая | 🟡 |
| **Prompt optimization** | `stanfordnlp/dspy` | Автоматический tuning | Очень высокая | 🟡 |
| **RAG для internal docs** | `run-llama/llama_index` | Верификация по внутр. базе | Средняя | 🟡 |

---

## Roadmap внедрения

### Фаза 1: Базовая функциональность (2-3 недели)

**Цель:** Превратить прототип в working solution

1. **Добавить LLM API интеграцию**
   - OpenAI / Anthropic / local model (llama.cpp)
   - Обновить все агенты для использования LLM вместо эвристик

2. **Реализовать веб-поиск в VerifierAgent**
   - LangChain + SerpAPI
   - Базовый ReAct-цикл

3. **Интегрировать PairRM в JudgeAgent**
   - Заменить `_evaluate_criterion()` на PairRM inference
   - Fallback на эвристики, если PairRM недоступен

4. **Добавить SelfCheckGPT в ExtractorAgent**
   - Multiple sampling для детекции галлюцинаций
   - Threshold-based filtering

**Dependencies для requirements.txt:**
```
# LLM APIs
openai>=1.0.0
anthropic>=0.3.0

# Reasoning & Orchestration
langchain>=0.1.0
langgraph>=0.0.20
langchain-community>=0.0.20

# Models
transformers>=4.36.0
torch>=2.0.0
sentence-transformers>=2.2.0

# Web search
google-search-results>=2.4.2  # SerpAPI

# Hallucination detection
selfcheckgpt>=0.1.0

# Existing
pandas>=2.0.0
numpy>=1.24.0
regex>=2023.0.0
```

---

### Фаза 2: Улучшение качества (2-3 недели)

**Цель:** Повысить robustness и качество

1. **Мигрировать на LangGraph**
   - Переписать `TextMergeOrchestrator` как StateGraph
   - Добавить условные ветвления
   - Retry-логика для failed steps

2. **Реализовать полный CoVe в VerifierAgent**
   - План верификации
   - Ответы на вопросы
   - Переписывание ответа

3. **Добавить CCoT во все промпты**
   - Обновить `prompts/*.md`
   - A/B тестирование с/без CoT

4. **Опционально: LLM-Blender GenFusion**
   - Для автоматического финального синтеза

---

### Фаза 3: Production hardening (1-2 недели)

**Цель:** Production-ready система

1. **Monitoring & Logging**
   - LangSmith интеграция
   - Метрики качества (accuracy, latency, cost)

2. **Error handling**
   - Graceful degradation (fallback на эвристики)
   - Retry с exponential backoff

3. **Cost optimization**
   - Кэширование LLM-ответов
   - Batching для PairRM inference

4. **Testing**
   - Unit tests для каждого агента
   - Integration tests для пайплайна
   - Golden dataset для regression testing

---

## Оценка трудозатрат

| Фаза | Задачи | Часы разработки | Стоимость API-тестирования |
|------|--------|-----------------|----------------------------|
| **Фаза 1** | LLM API + WebSearch + PairRM + SelfCheck | 40-60 | $50-100 (тестирование) |
| **Фаза 2** | LangGraph + CoVe + CCoT + GenFusion | 30-50 | $30-50 |
| **Фаза 3** | Production hardening | 20-30 | $20-40 |
| **ИТОГО** | | **90-140 часов** | **$100-190** |

**Предполагаемый срок:** 6-8 недель (1 разработчик full-time)

---

## Выводы

### ✅ Что КРИТИЧНО внедрить:

1. **ReAct + LangChain для VerifierAgent** — превращает заглушку в рабочую функцию
2. **PairRM для JudgeAgent** — заменяет эвристики на SOTA
3. **LangGraph для оркестратора** — делает систему production-ready
4. **SelfCheckGPT для ExtractorAgent** — предотвращает галлюцинации

### ⚠️ Что внедрять ОПЦИОНАЛЬНО:

- **ToT для JudgeAgent** — если нужно максимальное качество (высокая стоимость)
- **MoA** — если критична robustness (архитектурный редизайн)
- **AutoGen debate** — для разрешения спорных случаев
- **DSPy** — для долгосрочной оптимизации

### ❌ Что НЕ стоит внедрять:

- **LATS/ToT для ExtractorAgent** — избыточная сложность
- **Специализированный NLI для AlignerAgent** — PairRM покрывает

---

## Итоговый вердикт

**Текущий код:** Прототип с эвристиками, не production-ready

**После внедрения Tier 1:**
- ✅ Working solution с real LLM reasoning
- ✅ Реальная веб-верификация фактов
- ✅ SOTA парное ранжирование
- ✅ Production-ready оркестрация
- ✅ Детекция галлюцинаций

**ROI:** При ~90 часах разработки и <$200 тестирования получаем **полнофункциональную систему** вместо прототипа.

**Рекомендация:** Начать с **Tier 1 (Фаза 1)**, затем оценить необходимость Tier 2/3 на основе метрик качества.
